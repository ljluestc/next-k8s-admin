apiVersion: v1
kind: Service
metadata:
  name: next-k8s-admin
  labels:
    app.kubernetes.io/name: next-k8s-admin
spec:
  selector:
    app.kubernetes.io/name: next-k8s-admin
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
